---
const base = import.meta.env.BASE_URL;
---

<div class="navbar">

  <a href={base}>EASTSMP</a>

  <a href={`${base}rules/`}>Rules</a>

  <a href={`${base}roles/`}>Roles</a>

  <a href={`${base}updates/`}>Updates</a>

  <a href={`${base}information/`}>About the East SMP</a>

  <a href={`${base}story/`}>Story</a>

  <a href={`${base}applications/`}>Applications</a>

</div>

